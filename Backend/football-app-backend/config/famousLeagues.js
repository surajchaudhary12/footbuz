// config/famousLeagues.js

module.exports = {
  famousLeagueIds: [
    17, // Premier League (England)
    8, // LaLiga (Spain)
    35, // Bundesliga (Germany)
    23, // Serie A (Italy)
    34, // Ligue 1 (France)
    37, // Eredivisie (not present in JSON, assumed to be unchanged)
    24, // Primeira Liga (not present in JSON, assumed to be unchanged)
    21, // Süper Lig (not present in JSON, assumed to be unchanged)
    7, // Scottish Premiership (not present in JSON, assumed to be unchanged)
    93, // MLS (not present in JSON, assumed to be unchanged)
    // Add more league IDs as needed
  ],
};
