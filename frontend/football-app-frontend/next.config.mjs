/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "crests.football-data.org",
      "via.placeholder.com",
      "images.unsplash.com",
      "www.thesportsdb.com",
    ],
  },
};

export default nextConfig;
